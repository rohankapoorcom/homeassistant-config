friendly_name: "Upstairs Volume"
position_template: 50
icon_template: mdi:speaker
open_cover:
  service: script.harmony_volume
  data_template:
    vol_up: true
    increment: "{{ (states('input_number.upstairs_volume_increment'))|int }}"
    entity_id: remote.upstairs_hub
close_cover:
  service: script.harmony_volume
  data_template:
    increment: "{{ (states('input_number.upstairs_volume_increment'))|int }}"
    entity_id: remote.upstairs_hub
